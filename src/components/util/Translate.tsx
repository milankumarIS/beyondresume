import { Box } from "@mui/material";

export default function Translate() {
    // const { t } = useTranslation();
    // return <>{t(tkey)}</>
    return <Box sx={{height:'10px'}} id="google_translate_element"></Box>;
}