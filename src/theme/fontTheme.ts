// src/theme.ts
import { createTheme } from "@mui/material/styles";

const fontTheme = createTheme({
  typography: {
    fontFamily:
      "Custom-Regular,Custom-Bold,Montserrat-Regular, Arial, sans-serif,Custom-ExtraBold",
  },
});

export default fontTheme;
